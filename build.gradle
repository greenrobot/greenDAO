subprojects {
    repositories {
        mavenCentral()
        jcenter()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.9'
}

ext {
    buildToolsVersion = '23.0.2'
    compileSdkVersion = 23

    minSdkVersion = 4
    targetSdkVersion = 19
}